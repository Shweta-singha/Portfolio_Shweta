const Navbar = () => {
  return (
    <nav>
      <div>Jalaj</div>

      <div>
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
      </div>

      <div></div>
    </nav>
  );
};

export default Navbar;
